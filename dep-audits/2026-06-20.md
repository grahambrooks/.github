# Weekly Dependency Audit — 2026-06-20

> Generated by scheduled maintenance routine.
> **PRs could not be auto-created** — see "Action required" section at bottom.

---

## Go Repos

### `attribute` — Updates available (last commit May 24)

Direct dep: `stretchr/testify` v1.7.0 → **v1.11.1** (minor, safe)

Notable indirect updates:
- `golang.org/x/mod` v0.12.0 → v0.37.0
- `golang.org/x/tools` v0.11.0 → v0.46.0
- `golang.org/x/sync` v0.0.0-20190423 → v0.21.0
- `golang.org/x/sys` v0.45.0 → v0.46.0
- `golang.org/x/net` v0.55.0 → v0.56.0
- `github.com/gliderlabs/ssh` v0.2.2 → v0.3.8
- `github.com/google/go-cmp` v0.6.0 → v0.7.0

Run: `go get -u ./... && go mod tidy`

---

### `ai-dev-container` — Updates available (last commit May 25)

| Package | Current | Latest |
|---|---|---|
| `github.com/moby/moby/api` | v1.54.0 | **v1.55.0** |
| `github.com/moby/moby/client` | v0.3.0 | **v0.5.0** |
| `github.com/docker/go-connections` | v0.6.0 | **v0.7.0** |
| `go.opentelemetry.io/otel` | v1.42.0 | v1.44.0 |
| `go.opentelemetry.io/otel/sdk` | v1.42.0 | v1.44.0 |
| `go.opentelemetry.io/contrib/...otelhttp` | v0.67.0 | v0.69.0 |
| `github.com/containerd/typeurl/v2` | v2.2.0 | v2.3.0 |
| `golang.org/x/sys` | v0.42.0 | v0.46.0 |

Run: `go get -u ./... && go mod tidy`

---

### `bsdoc` — Skip major; minor indirect updates (last commit May 24)

- `github.com/github/copilot-sdk/go` v0.3.0 → v1.0.2 (**MAJOR** — skip)
- `go.opentelemetry.io/otel` v1.43.0 → v1.44.0 (indirect minor)
- `github.com/klauspost/compress` v1.18.3 → v1.18.6 (indirect patch)

Only indirect minor/patch updates are available (major skipped). Low priority.

---

### `fingerprint` — Up to date (last commit Jun 19)

Only indirect patch bumps (`stretchr/objx` v0.5.2→v0.5.3). No action needed.

---

### `scheme` — Up to date (last commit Apr 25)

Only indirect patch bumps. No action needed.

---

## Rust Repos (Cargo.lock updates via `cargo update`)

### `colab` — ~14 lock-file updates (last commit Jun 11)

Notable: `jiff` v0.2.24→v0.2.28, `chrono` v0.4.44→v0.4.45, `bitflags` v2.11.1→v2.13.0, `bytes` v1.11.1→v1.12.0, `log` v0.4.29→v0.4.32, `assert_cmd` v2.2.1→v2.2.2, `ignore` v0.4.25→v0.4.26

Run: `cargo update`

---

### `gitatlas-cli` — ~15 lock-file updates (last commit May 24)

Notable: `libgit2-sys` v0.18.4→v0.18.5, `lru` v0.16.4→v0.18.0, `chrono` v0.4.44→v0.4.45, `bitflags` v2.11.1→v2.13.0, `libz-sys` v1.1.28→v1.1.29, `compact_str` v0.9.0→v0.9.1, `log` v0.4.29→v0.4.32

Run: `cargo update`

---

### `mcpm` — ~20+ lock-file updates (last commit Apr 25)

Notable: `clap` v4.5.57→v4.6.1, `anstream` v0.6.21→v1.0.0, `aws-lc-rs` v1.15.4→v1.17.0, `aws-lc-sys` v0.37.0→v0.41.0, `bytes` v1.11.1→v1.12.0, `anyhow` v1.0.101→v1.0.102, `compact_str` v0.9.0→v0.9.1

Run: `cargo update`

---

### `bsv` — ~20 lock-file updates (last commit Apr 27)

Notable: `aws-lc-rs` v1.16.3→v1.17.0, `fancy-regex` v0.17.0→v0.18.0, `h2` v0.4.13→v0.4.15, `hyper` v1.9.0→v1.10.1, `jni` v0.21.1→v0.22.4, `bytes` v1.11.1→v1.12.0, `bitflags` v2.11.1→v2.13.0

Run: `cargo update`

---

### `genie` — ~10 lock-file updates (last commit Apr 25)

Notable: `hyper` v1.9.0→v1.10.1, `h2` v0.4.13→v0.4.15, `http` v1.4.0→v1.4.2, `chrono` v0.4.44→v0.4.45, `bytes` v1.11.1→v1.12.0

Run: `cargo update`

---

### `cic` — ~10 lock-file updates (last commit Apr 25)

Notable: `hyper` v1.9.0→v1.10.1, `http` v1.4.0→v1.4.2, `compact_str` v0.8.1→v0.8.2, `chrono` v0.4.44→v0.4.45, `bytes` v1.11.1→v1.12.0

Run: `cargo update`

---

### `astgen` — ~10 lock-file updates (last commit Apr 25)

Notable: `dashmap` v6.1.0→v6.2.1, `jiff` v0.2.24→v0.2.28, `regex` v1.12.3→v1.12.4, `ignore` v0.4.25→v0.4.26, `log` v0.4.29→v0.4.32

Run: `cargo update`

---

### `adoc` — ~10 lock-file updates (last commit Apr 29)

Notable: `serde_json` v1.0.149→v1.0.150, `insta` v1.47.2→v1.48.0, `syn` v2.0.117→v2.0.118, `regex-syntax` v0.8.10→v0.8.11, `bitflags` v2.11.1→v2.13.0

Run: `cargo update`

---

### `puml` — ~10 lock-file updates (last commit May 24)

Notable: `insta` v1.47.2→v1.48.0, `kqueue` v1.1.1→v1.2.0, `mio` v1.2.0→v1.2.1, `inotify` v0.11.1→v0.11.2, `memchr` v2.8.0→v2.8.2, `bitflags` v2.11.1→v2.13.0

Run: `cargo update`

---

### `bx`, `casual-review` — Cargo.lock current; major version available

- `thiserror` v1.0.x → v2.0.18 available (MAJOR — skip per policy)
- All other Cargo.lock deps are current

---

### Already up to date (Rust)

`symgraph`, `refactor-dsl`, `mcp-dep`, `rv`, `facts` — no Cargo.lock updates available.

---

## JS / TS Repos

### `gitatlas` — No updates needed

All semver ranges (`^2`, `^19`, `^4`, `^6`) already resolve to latest compatible versions.

---

## Private Repos (could not clone — need credentials)

- `snag`
- `insight`
- `structurizr-lsp`
- `code-provenance`

These are private and were skipped. Run maintenance manually or configure the scheduled session with a PAT that has read/write access.

---

## Also Skipped

- `forge` — Rust workspace in `forge/` subdirectory; complex structure, skipped conservatively.
- `product-backstage` — Large Backstage monorepo with yarn workspaces; consider Renovate bot.
- `codecity` — frontend `package.json` uses semver ranges; check with `npm outdated` in `frontend/`.
- `casual-review` — VSCode extension `extensions/vscode/package.json`; check manually.
- `grahambrooks.blog`, `grahambrooks.github.io` — Static sites; no dependency manifests.
- `.github`, `gb-agent-skills`, `insight-agents`, `mermaid-adr`, `accounting-pattern` — No dependency manifests.

---

## Action Required: Fix Session Scope for Future Runs

**PRs were not created.** The scheduled session's GitHub MCP is scoped only to `grahambrooks/.github`. To push maintenance branches and open PRs across repos, the session needs write access to each target repo.

**To fix:** In Claude Code on the web session settings, add each target repository to the allowed scope, or provide a GitHub PAT with `repo` scope as an environment variable.

**To apply updates manually right now:**

```bash
# Go repos with meaningful updates:
cd ~/repos/attribute && git checkout -b maintenance/deps-2026-06-20 && go get -u ./... && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-06-20

cd ~/repos/ai-dev-container && git checkout -b maintenance/deps-2026-06-20 && go get -u ./... && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-06-20

# Rust repos with Cargo.lock updates:
for repo in colab gitatlas-cli mcpm bsv genie cic astgen adoc puml; do
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-06-20 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-06-20
done
```
