# Weekly Dependency Audit — 2026-07-25

> Generated by scheduled maintenance routine.
> **PRs could not be auto-created** — GitHub MCP is scoped to `grahambrooks/.github` only; write access to other repos was denied (403). See "Action Required" at bottom.

---

## Rust Repos (`cargo update` — Cargo.lock updates only)

### `adoc` — 26 lock-file updates ✅ clean
No major version jumps. Notable: `bitflags` v2.11.1→v2.13.1, `camino` v1.2.2→v1.2.4, `clap` v4.6.1→v4.6.4, `console` v0.16.3→v0.16.4, `fastrand` v2.4.1→v2.5.0, `getrandom` v0.4.2→v0.4.3, `insta` v1.47.2→v1.48.0, `libc` v0.2.186→v0.2.189, `zmij` v1.0.21→v1.0.23.

```bash
cd ~/repos/adoc && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-07-25
```

---

### `puml` — 29 lock-file updates ✅ clean
No major version jumps. Notable: `bitflags` v2.11.1→v2.13.1, `insta` v1.47.2→v1.48.0, `kqueue` v1.1.1→v1.2.0, `mio` v1.2.0→v1.2.1, `inotify` v0.11.1→v0.11.2, `libc` v0.2.186→v0.2.189.

```bash
cd ~/repos/puml && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-07-25
```

---

### `fingerprint-rs` — 3 updates, 1 major ⚠️
Updates: `clap` v4.6.2→v4.6.4, `clap_derive` v4.6.1→v4.6.4 ✅

**Skip major:** `syn` v2.0.119→v3.0.3 (major — proc-macro API changes; review before updating)

To apply minor/patch only:
```bash
cd ~/repos/fingerprint-rs && cargo update --precise 'clap@4.6.4' --precise 'clap_derive@4.6.4'
```

---

### `astgen` — 52 updates, 2 majors ⚠️
Skipping: `env_filter` v1.0→v2.0, `shlex` v1.3→v2.0.
Remaining updates are minor/patch (bstr, bumpalo, cc, clap, crossbeam, etc.).

```bash
cd ~/repos/astgen && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: env_filter 1.0→2.0, shlex 1.3→2.0 (allowed by constraints)' && git push -u origin maintenance/deps-2026-07-25
```

---

### `bsv` — 61 updates, 1 major ⚠️
Skipping: `aws-lc-sys` v0.41→v0.43 (0.x minor-as-major).
Remaining: anyhow, aws-lc-rs, bitflags, bytemuck, bytes, cc, etc.

```bash
cd ~/repos/bsv && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: aws-lc-sys 0.41→0.43 (0.x)' && git push -u origin maintenance/deps-2026-07-25
```

---

### `cic` — 96 updates, 5 majors ⚠️
Skipping: `getrandom` v0.3→v0.4, `r-efi` v5.3→v6.0, `rand` v0.9→v0.10, `rand_core` v0.9→v0.10, `shlex` v1.3→v2.0.

**Note:** `rand` 0.9→0.10 is a significant ecosystem change. Review carefully before applying.

```bash
cd ~/repos/cic && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: getrandom 0.3→0.4, r-efi 5.3→6.0, rand 0.9→0.10, rand_core 0.9→0.10, shlex 1.3→2.0' && git push -u origin maintenance/deps-2026-07-25
```

---

### `colab` — 75 updates, 2 majors ⚠️
Skipping: `env_filter` v1.0→v2.0, `shlex` v1.3→v2.0.
Remaining: assert_cmd, async-trait, autocfg, bitflags, bstr, bumpalo, bytes, cc, chrono, clap, etc.

```bash
cd ~/repos/colab && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: env_filter 1.0→2.0, shlex 1.3→2.0' && git push -u origin maintenance/deps-2026-07-25
```

---

### `genie` — 93 updates, 4 majors ⚠️
Skipping: `cpufeatures` v0.2→v0.3, `nix` v0.30→v0.31, `rand_core` v0.9→v0.10, `shlex` v1.3→v2.0.

```bash
cd ~/repos/genie && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: cpufeatures 0.2→0.3, nix 0.30→0.31, rand_core 0.9→0.10, shlex 1.3→2.0' && git push -u origin maintenance/deps-2026-07-25
```

---

### `gitatlas-cli` — 77 updates, 4 majors ⚠️
Skipping: `lru` v0.16→v0.18, `shlex` v1.3→v2.0, `strum` v0.27→v0.28, `strum_macros` v0.27→v0.28.

```bash
cd ~/repos/gitatlas-cli && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: lru 0.16→0.18, shlex 1.3→2.0, strum 0.27→0.28, strum_macros 0.27→0.28' && git push -u origin maintenance/deps-2026-07-25
```

---

### `mcpm` — 142 updates, 14 majors ⚠️
This repo has the most accumulated drift. Notable minor/patch: anstyle, anyhow, async-trait, aws-lc-rs, bitflags, bumpalo, bytes, cc, clap, etc.

**Skip majors:** anstream 0.6→1.0, anstyle-parse 0.2→1.0, aws-lc-sys 0.37→0.43, clap_lex 0.7→1.1, jni 0.21→0.22, jni-sys 0.3→0.4, linux-raw-sys 0.11→0.12, lru 0.16→0.18, rand_core 0.9→0.10, rustls-platform-verifier 0.6→0.7, shlex 1.3→2.0, strum 0.27→0.28, strum_macros 0.27→0.28, wit-bindgen 0.51→0.57.

```bash
cd ~/repos/mcpm && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors (14): anstream 0.6→1.0, anstyle-parse 0.2→1.0, aws-lc-sys 0.37→0.43, clap_lex 0.7→1.1, jni 0.21→0.22, jni-sys 0.3→0.4, linux-raw-sys 0.11→0.12, lru 0.16→0.18, rand_core 0.9→0.10, rustls-platform-verifier 0.6→0.7, shlex 1.3→2.0, strum 0.27→0.28, strum_macros 0.27→0.28, wit-bindgen 0.51→0.57' && git push -u origin maintenance/deps-2026-07-25
```

---

### `snag` — 93 updates, 2 majors ⚠️
Skipping: `env_filter` v1.0→v2.0, `shlex` v1.3→v2.0.
Remaining: anyhow, arc-swap, arrayvec, async-trait, autocfg, bstr, bumpalo, bytes, bytesize, cc, etc.

```bash
cd ~/repos/snag && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: env_filter 1.0→2.0, shlex 1.3→2.0' && git push -u origin maintenance/deps-2026-07-25
```

---

### Already up to date (Rust)
`facts`, `bx`, `casual-review`, `mcp-dep`, `refactor-dsl`, `rv`, `symgraph` — no Cargo.lock updates available.

---

## Go Repos (`go get -u ./... && go mod tidy`)

### `ai-dev-container` — 25 updates ✅ clean
| Package | Current | Latest |
|---|---|---|
| `github.com/moby/moby/api` | v1.54.0 | **v1.55.0** |
| `github.com/moby/moby/client` | v0.3.0 | **v0.5.0** |
| `github.com/docker/go-connections` | v0.6.0 | **v0.7.0** |
| `go.opentelemetry.io/otel` | v1.42.0 | v1.44.0 |
| `go.opentelemetry.io/otel/sdk` | v1.42.0 | v1.44.0 |
| `go.opentelemetry.io/contrib/.../otelhttp` | v0.67.0 | v0.69.0 |
| `github.com/containerd/typeurl/v2` | v2.2.0 | v2.3.0 |
| `golang.org/x/sys` | v0.42.0 | v0.47.0 |
| `golang.org/x/mod` | v0.12.0 | v0.38.0 |
| `golang.org/x/term` | v0.41.0 | v0.45.0 |

```bash
cd ~/repos/ai-dev-container && git checkout -b maintenance/deps-2026-07-25 && go get -u ./... && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-07-25
```

---

### `attribute` — 22 updates ✅ clean
Notable direct: `stretchr/testify` v1.7.0→**v1.11.1** (4 minor versions of drift).
Notable indirect: `golang.org/x/mod` v0.12→v0.38, `golang.org/x/tools` v0.11→v0.48, `golang.org/x/sync` v0.0.0-2019→v0.22.0, `gliderlabs/ssh` v0.2.2→v0.3.8, `google/go-cmp` v0.6→v0.7, `golang.org/x/sys` v0.45→v0.47.

```bash
cd ~/repos/attribute && git checkout -b maintenance/deps-2026-07-25 && go get -u ./... && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-07-25
```

---

### `bsdoc` — 8 updates, 1 major ⚠️
**Skip major:** `github.com/github/copilot-sdk/go` v0.3.0→**v1.0.8** (major — API likely changed).
Minor/patch available: `go.opentelemetry.io/otel` v1.43→v1.44, `klauspost/compress` v1.18.3→v1.19.1, `rogpeppe/go-internal` v1.14.1→v1.15.0, `go-logr/logr` v1.4.3→v1.4.4.

To update only minor/patch (manually):
```bash
cd ~/repos/bsdoc && go get go.opentelemetry.io/otel@latest go.opentelemetry.io/otel/metric@latest go.opentelemetry.io/otel/trace@latest github.com/klauspost/compress@latest && go mod tidy
```

---

### `fingerprint` — 2 updates (patches only)
`stretchr/objx` v0.5.2→v0.5.3. Also `gopkg.in/check.v1` v0.0.0-20161208→v1.0.0 but this is 0.x→1.x (skip per policy).

Low priority — only indirect test-helper patches.

---

### `scheme` — 3 updates (patches only)
`stretchr/objx` v0.5.2→v0.5.3, `golang.org/x/sys` v0.40→v0.47. `gopkg.in/check.v1` 0.x→1.x (skip). Low priority.

---

## JS / TS Repos

### `gitatlas` (npm, Tauri + React) — Up to date
All ranges (`^2`, `^19.0`, `^4`, `^6`) resolve to their latest compatible versions. No `npm update` needed.

---

### `product-backstage` (yarn berry, Backstage monorepo) — Skipped
Complex multi-package workspace. Backstage versions are tightly coupled across `@backstage/*` packages. Consider [Backstage version bump script](https://backstage.io/docs/getting-started/keeping-backstage-updated/) or Renovate bot for this one.

---

## Repos Not Accessible (Likely Private)

These repos returned a 403 during clone and could not be audited:
`ggh`, `bazelviz`, `grahambrooks.github.io`, `grahambrooks.blog`, `code-provenance`, `drive`, `insight-agents`, `insight`, `forge`, `kaizen-ai`

---

## No Dependency Manifests (Skipped)

`codecity`, `gb-agent-skills`, `mermaid-adr` — no recognized package manager files found.

---

## Action Required: Fix Session Scope

**Root cause:** The GitHub MCP server in the scheduled session is scoped only to `grahambrooks/.github`. `git push` to all other repos returned HTTP 403.

**To apply the updates:** Clone locally (or on a session with broader scope) and run the bash one-liners above for each repo. PRs can then be opened from those branches.

**To fix for future runs:** In Claude Code → session settings for the scheduled task, add each target repo to the allowed MCP scope, or pass a `GITHUB_TOKEN` env variable with `repo` write access.

### Quick apply all (copy-paste):

```bash
# Rust - adoc, puml (clean, no majors)
for repo in adoc puml; do
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-07-25
done

# Rust - repos with major bumps (cargo update applies everything within constraints; majors noted in commit)
for repo in astgen bsv cic colab genie gitatlas-cli mcpm snag; do
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-07-25 && cargo update && git commit -am 'chore(deps): weekly dependency updates (see commit for skipped majors)' && git push -u origin maintenance/deps-2026-07-25
done

# Go - ai-dev-container, attribute (clean)
for repo in ai-dev-container attribute; do
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-07-25 && go get -u ./... && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-07-25
done
```
