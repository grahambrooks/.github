# Weekly Dependency Audit — 2026-08-01

> Generated by scheduled maintenance routine.
> **PRs could not be auto-created** — `git push` to all repos other than `grahambrooks/.github` returns HTTP 403 (session scope). See "Action Required" at bottom for the fix and copy-paste commands.

---

## Rust Repos (`cargo update --dry-run`)

### `adoc` — 20 minor/patch updates ✅ clean

All within semver constraints. Notable: `bitflags` v2.11.1→v2.13.1, `clap` v4.6.4→v4.6.5, `getrandom` v0.4.2→v0.4.3, `libc` v0.2.186→v0.2.189, `log` v0.4.29→v0.4.33, `memchr` v2.8.0→v2.8.3, `proc-macro2` v1.0.106→v1.0.107, `syn` v2.0.117→v2.0.119, `zmij` v1.0.21→v1.0.23.

```bash
cd ~/repos/adoc && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `bsv` — 3 updates ✅ clean

`displaydoc` v0.2.6→v0.2.7, `http` v1.4.2→v1.5.0, `rustls` v0.23.42→v0.23.43.

```bash
cd ~/repos/bsv && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `fingerprint-rs` — 2 updates ✅ clean

`clap` v4.6.4→v4.6.5, `clap_builder` v4.6.2→v4.6.5.

```bash
cd ~/repos/fingerprint-rs && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `gitatlas-cli` — 3 updates ✅ clean

`clap` v4.6.4→v4.6.5, `clap_builder` v4.6.2→v4.6.5, `displaydoc` v0.2.6→v0.2.7.

```bash
cd ~/repos/gitatlas-cli && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `puml` — 2 updates ✅ clean

`clap` v4.6.4→v4.6.5, `clap_builder` v4.6.2→v4.6.5.

```bash
cd ~/repos/puml && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `astgen` — 33 updates, 1 major ⚠️

**Skip major (indirect):** `shlex` v1.3.0→v2.0.1 (major — transitive dep, conservative skip).

All other updates are minor/patch: `bstr`, `bumpalo`, `cc` v1.2.61→v1.4.0, `clap`, `crossbeam-*`, `either`, `fastrand`, `getrandom`, `globset`, `jiff` v0.2.24→v0.2.35, `js-sys`, `libc`, `memchr`, `portable-atomic`, `proc-macro2`, `quote`, `rustversion`, `smallvec`, `syn`, `thiserror`, `wasm-bindgen`, `winnow`, `zmij`.

```bash
cd ~/repos/astgen && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped major (indirect): shlex 1.3.0→2.0.1' && git push -u origin maintenance/deps-2026-08-01
```

---

### `colab` — 53 updates, 1 major ⚠️

**Skip major (indirect):** `shlex` v1.3.0→v2.0.1.

All other updates are minor/patch: `async-trait`, `autocfg`, `bitflags`, `bstr`, `bumpalo`, `bytes` v1.11.1→v1.12.1, `cc`, `clap`, `crossbeam-*`, `displaydoc`, `either`, `futures` v0.3.32→v0.3.33, `jiff`, `js-sys`, `libc`, `memchr`, `mio` v1.2.0→v1.2.2, `pin-project`, `portable-atomic`, `proc-macro2`, `quote`, `regex` v1.12.3→v1.13.1, `rustversion`, `serde_repr`, `smallvec`, `socket2`, `syn`, `tokio-macros`, `tokio-util`, `wasm-bindgen`, `yoke`, `zerocopy` v0.8.48→v0.8.55, `zmij`.

```bash
cd ~/repos/colab && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped major (indirect): shlex 1.3.0→2.0.1' && git push -u origin maintenance/deps-2026-08-01
```

---

### `genie` — 49 updates, 1 major ⚠️

**Skip major (indirect):** `shlex` v1.3.0→v2.0.1.

Notable minor/patch: `async-openai` v0.41.2→v0.41.3, `bytes`, `cc`, `clap`, `displaydoc`, `fastrand`, `getrandom`, `http` v1.4.0→v1.5.0, `http-body` v1.0.1→v1.1.0, `libc`, `mio`, `quinn` v0.11.9→v0.11.11, `rustls` v0.23.39→v0.23.43, `rustls-native-certs`, `rustls-pki-types`, `socket2`, `thiserror`, `tower-http` v0.6.8→v0.6.11, `wasm-bindgen`, `zeroize` v1.8.2→v1.9.0, `zmij`.

```bash
cd ~/repos/genie && git checkout -b maintenance/deps-2026-08-01 && cargo update && git commit -am 'chore(deps): weekly dependency updates

Skipped major (indirect): shlex 1.3.0→2.0.1' && git push -u origin maintenance/deps-2026-08-01
```

---

### Already up to date (Rust)

`bx`, `casual-review`, `cic`, `facts`, `mcp-dep`, `refactor-dsl`, `rv`, `symgraph` — no Cargo.lock updates available.

---

## Go Repos (`go list -u -m all`)

### `ai-dev-container` — 15 updates ✅ clean

| Package | Current | Latest |
|---|---|---|
| `github.com/containerd/typeurl/v2` | v2.2.0 | v2.3.0 |
| `github.com/cpuguy83/go-md2man/v2` | v2.0.6 | v2.0.7 |
| `github.com/docker/go-connections` | v0.7.0 | **v0.8.1** |
| `github.com/felixge/httpsnoop` | v1.0.4 | v1.1.0 |
| `github.com/go-logr/logr` | v1.4.3 | v1.4.4 |
| `github.com/rogpeppe/go-internal` | v1.14.1 | v1.15.0 |
| `github.com/sirupsen/logrus` | v1.9.3 | v1.9.4 |
| `github.com/stretchr/objx` | v0.5.2 | v0.5.3 |
| `go.opentelemetry.io/otel` | v1.42.0 | v1.44.0 |
| `go.opentelemetry.io/otel/{metric,sdk,trace}` | v1.42.0 | v1.44.0 |
| `go.yaml.in/yaml/v3` | v3.0.4 | v3.0.5 |
| `go.opentelemetry.io/contrib/.../otelhttp` | v0.67.0 | v0.69.0 |

```bash
cd ~/repos/ai-dev-container && git checkout -b maintenance/deps-2026-08-01 && go get -u ./... && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `attribute` — 15 updates ✅ clean

| Package | Current | Latest |
|---|---|---|
| `github.com/gliderlabs/ssh` | v0.2.2 | v0.3.8 |
| `github.com/jessevdk/go-flags` | v1.4.0 | v1.6.1 |
| `github.com/cpuguy83/go-md2man/v2` | v2.0.6 | v2.0.7 |
| `github.com/creack/pty` | v1.1.9 | v1.1.24 |
| `github.com/google/go-cmp` | v0.6.0 | v0.7.0 |
| `github.com/kr/pretty` | v0.1.0 | v0.3.1 |
| `github.com/pkg/errors` | v0.8.1 | v0.9.1 |
| `github.com/sirupsen/logrus` | v1.9.3 | v1.9.4 |
| `github.com/stretchr/objx` | v0.5.2 | v0.5.3 |
| `go.yaml.in/yaml/v3` | v3.0.4 | v3.0.5 |
| `golang.org/x/crypto` | v0.52.0 | v0.54.0 |
| `golang.org/x/mod` | v0.12.0 | **v0.38.0** |
| `golang.org/x/net` | v0.55.0 | v0.57.0 |

```bash
cd ~/repos/attribute && git checkout -b maintenance/deps-2026-08-01 && go get -u ./... && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `bsdoc` — 8 updates, 1 major ⚠️

**Skip major:** `github.com/github/copilot-sdk/go` v0.3.0→v1.0.8 (major version change — API likely changed).

Minor/patch only: `cpuguy83/go-md2man` v2.0.6→v2.0.7, `go-logr/logr` v1.4.3→v1.4.4, `klauspost/compress` v1.18.3→v1.19.1, `rogpeppe/go-internal` v1.14.1→v1.15.0, `go.opentelemetry.io/otel` v1.43.0→v1.44.0, `go.yaml.in/yaml/v3` v3.0.4→v3.0.5.

```bash
cd ~/repos/bsdoc && git checkout -b maintenance/deps-2026-08-01 && go get \
  github.com/cpuguy83/go-md2man/v2@latest \
  github.com/go-logr/logr@latest \
  github.com/klauspost/compress@latest \
  github.com/rogpeppe/go-internal@latest \
  go.opentelemetry.io/otel@latest \
  go.opentelemetry.io/otel/metric@latest \
  go.opentelemetry.io/otel/trace@latest \
  go.yaml.in/yaml/v3@latest && go mod tidy && git commit -am 'chore(deps): weekly dependency updates

Skipped major: github/copilot-sdk/go 0.3→1.0.8' && git push -u origin maintenance/deps-2026-08-01
```

---

### `fingerprint` — 1 update (patch only)

`stretchr/objx` v0.5.2→v0.5.3. Low priority (indirect test dep).
`gopkg.in/check.v1` 0.x→1.x **skipped** (0→1 major).

Low priority — not worth a standalone PR.

---

### `scheme` — 2 updates (minor/patch)

`stretchr/objx` v0.5.2→v0.5.3, `golang.org/x/sys` v0.40.0→v0.47.0.
`gopkg.in/check.v1` 0.x→1.x **skipped**.

```bash
cd ~/repos/scheme && git checkout -b maintenance/deps-2026-08-01 && go get golang.org/x/sys@latest github.com/stretchr/objx@latest && go mod tidy && git commit -am 'chore(deps): weekly dependency updates' && git push -u origin maintenance/deps-2026-08-01
```

---

### `xpa` — 6 minor/patch updates, 2 majors ⚠️

**Skip majors:** `antlr4` v0.0.0→v4.11.1+incompatible (significant API change); `gopkg.in/check.v1` 0.x→1.x.

Minor/patch: `go.yaml.in/yaml/v3` v3.0.4→v3.0.5, `golang.org/x/sys` v0.42.0→v0.47.0, `mattn/go-colorable` v0.1.14→v0.1.15, `mattn/go-isatty` v0.0.20→v0.0.24, `spf13/pflag` v1.0.9→v1.0.10, `cpuguy83/go-md2man` v2.0.6→v2.0.7.

```bash
cd ~/repos/xpa && git checkout -b maintenance/deps-2026-08-01 && go get \
  go.yaml.in/yaml/v3@latest \
  golang.org/x/sys@latest \
  github.com/mattn/go-colorable@latest \
  github.com/mattn/go-isatty@latest \
  github.com/spf13/pflag@latest \
  github.com/cpuguy83/go-md2man/v2@latest && go mod tidy && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: antlr4 0.0.0→4.11.1+incompatible, gopkg.in/check.v1 0.x→1.x' && git push -u origin maintenance/deps-2026-08-01
```

---

### `bazel-mono` — No external module dependencies

---

## JS / Node Repos

### `gitatlas` (npm, Tauri + React) — All updates are MAJOR ⚠️

| Package | Current | Latest |
|---|---|---|
| `@vitejs/plugin-react` | 4.7.0 | **6.0.5** |
| `typescript` | 5.9.3 | **7.0.2** |
| `vite` | 6.4.3 | **8.2.0** |

All three are major version bumps — skipped per policy. Current `wanted` versions match current (no semver-range updates available). **Review manually.**

---

### `product-backstage` (yarn berry, Backstage monorepo) — Skipped

Complex multi-package Backstage workspace. Versions are tightly coupled across `@backstage/*` packages. Use [Backstage version bump script](https://backstage.io/docs/getting-started/keeping-backstage-updated/) or Renovate bot. **Review manually.**

---

## No Dependency Manifests (Skipped)

`bazel-mono` (Go but no external deps), `codecity`, `forge`, `gb-agent-skills`, `homebrew-genie`, `testing-logging` — no recognized package manager files found.

---

## Private Repos (Cloning Failed — Couldn't Audit)

`auto-managed`, `bazelviz`, `building-with-bazel`, `code-provenance`, `drive`, `eka`, `ggh`, `insight`, `insight-agents`, `kaizen-ai`, `mdbook-blog`, `grahambrooks.blog`, `grahambrooks.com`, `rebel-zebra`, `structurizr-lsp`

---

## Action Required: Fix Session Scope

**Root cause:** The scheduled Claude Code session is scoped only to `grahambrooks/.github`. `git push` to all other repos returns HTTP 403.

**To apply the updates now:** Clone locally and run the bash one-liners above for each repo. PRs can then be opened from those branches.

**To fix for future runs:** In Claude Code → session settings for the scheduled task, add each target repo to the allowed repo scope, or provide a `GITHUB_TOKEN` env variable with `repo` write access across all target repos.

### Summary: repos with ready-to-apply updates

| Repo | Type | Updates | Action |
|---|---|---|---|
| `adoc` | Rust | 20 minor/patch | Apply |
| `bsv` | Rust | 3 minor/patch | Apply |
| `fingerprint-rs` | Rust | 2 minor/patch | Apply |
| `gitatlas-cli` | Rust | 3 minor/patch | Apply |
| `puml` | Rust | 2 minor/patch | Apply |
| `astgen` | Rust | 33 minor/patch + 1 noted major | Apply (majors noted in commit) |
| `colab` | Rust | 53 minor/patch + 1 noted major | Apply |
| `genie` | Rust | 49 minor/patch + 1 noted major | Apply |
| `ai-dev-container` | Go | 15 minor/patch | Apply |
| `attribute` | Go | 15 minor/patch | Apply |
| `bsdoc` | Go | 7 minor/patch + 1 skipped major | Apply (manual, skip copilot-sdk) |
| `scheme` | Go | 2 minor/patch | Apply |
| `xpa` | Go | 6 minor/patch + 2 skipped majors | Apply (manual, skip antlr4+check) |
| `gitatlas` | Node | 0 (all 3 are major) | Manual review |
| `product-backstage` | Node | — | Manual review |

### Quick apply all Rust (copy-paste):

```bash
# Clean minor/patch (no majors)
for repo in adoc bsv fingerprint-rs gitatlas-cli puml; do
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-08-01 \
    && cargo update \
    && git commit -am 'chore(deps): weekly dependency updates' \
    && git push -u origin maintenance/deps-2026-08-01
done

# With noted indirect major (shlex 1→2)
for repo in astgen colab genie; do
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-08-01 \
    && cargo update \
    && git commit -am $'chore(deps): weekly dependency updates\n\nSkipped major (indirect): shlex 1.3.0→2.0.1' \
    && git push -u origin maintenance/deps-2026-08-01
done
```

### Quick apply all Go (copy-paste):

```bash
# Clean minor/patch
for repo in ai-dev-container attribute; do
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-08-01 \
    && go get -u ./... && go mod tidy \
    && git commit -am 'chore(deps): weekly dependency updates' \
    && git push -u origin maintenance/deps-2026-08-01
done

# scheme (minor/patch only)
cd ~/repos/scheme && git checkout -b maintenance/deps-2026-08-01 \
  && go get golang.org/x/sys@latest github.com/stretchr/objx@latest \
  && go mod tidy \
  && git commit -am 'chore(deps): weekly dependency updates' \
  && git push -u origin maintenance/deps-2026-08-01
```
