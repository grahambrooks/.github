# Weekly Dependency Audit — 2026-09-05

> Generated by scheduled maintenance routine.
> **PRs could not be auto-created** — session scope is limited to `grahambrooks/.github`. Apply updates with the bash one-liners below.

---

## Status vs Prior Week (2026-08-08)

| Item | Status |
|------|--------|
| `bx` reqwest feature flag fix | ✅ **RESOLVED** — now uses `rustls-no-provider` correctly |
| `cic` reqwest feature flag fix | ❌ **Still broken** — same error (see below) |
| Go updates (`ai-dev-container`, `attribute`, `bsdoc`, `xpa`) | ❌ **Not applied** — accumulated another month; versions moved again |
| `gitatlas` major updates | ✅ Renovate has open PRs (typescript-7.x, vite-8.x, plugin-react-6.x) |

---

## Rust Repos (`cargo update --dry-run`)

### Up to date ✅
`facts`

### Routine minor/patch updates available

All the following can be updated with `cd ~/repos/<repo> && cargo update && cargo test`:

| Repo | Updates | Notable packages |
|------|---------|-----------------|
| `adoc` | 2 | smallvec v1.15→v1.16, syn v3.0.4→v3.0.5 |
| `fingerprint-rs` | 1 | syn v3.0.4→v3.0.5 |
| `colab` | 12 | cc, hermit-abi, js-sys, mio, smallvec, syn, wasm-bindgen×3 |
| `astgen` | 12 | cc, hermit-abi, js-sys, libredox, portable-atomic-util, smallvec, syn, wasm-bindgen×4 |
| `gitatlas-cli` | 10 | cc, js-sys, lru, mio, syn, wasm-bindgen×3 |
| `puml` | 10 | crc32fast, flate2, font-types, log, mio, smallvec, syn, tinyvec + zlib-rs added |
| `bsv` | 16 | aws-lc-rs, aws-lc-sys, cc, js-sys, lru, mio, smallvec, syn, tokio-rustls, wasm-bindgen×4, web-sys |
| `genie` | 16 | aws-lc-rs, aws-lc-sys, cc, js-sys, mio, smallvec, syn, tinyvec, tokio-rustls, wasm-bindgen×4, web-sys |

```bash
# Quick apply all routine Rust updates:
for repo in adoc fingerprint-rs colab astgen gitatlas-cli puml bsv genie; do
  echo "--- $repo ---"
  cd ~/repos/$repo && git checkout -b maintenance/deps-2026-09-05 \
    && cargo update && git add Cargo.lock \
    && git commit -m 'chore(deps): weekly dependency updates

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TcCwPyJJfUT3nwmpGMovvM' \
    && git push -u origin maintenance/deps-2026-09-05
done
```

### Cannot check — Rust 1.98 toolchain download fails in session env ⚠️

`brake`, `increment`, `tropism` pin to `channel = "1.98"` via `rust-toolchain.toml`. Rustup cannot download the toolchain in this environment (rename error on partial download). Check locally.

### Requires manual action

**`cic`** — reqwest feature flag incompatibility (persisting since 2026-08-08):

```
error: package `cic` depends on `reqwest` with feature `rustls-tls`
       but `reqwest` does not have that feature.
       Use `rustls` instead.
```

Fix:
```bash
cd ~/repos/cic && git checkout -b fix/reqwest-feature-flags
# In Cargo.toml: change feature "rustls-tls" → "rustls"
cargo build && git commit -am 'fix(deps): reqwest feature rustls-tls → rustls'
git push -u origin fix/reqwest-feature-flags
```

**`mcp-dep`** — `rmcp` MAJOR version available: v0.3.2 → v3.2.0. API likely changed significantly. Manual review required before upgrading.

**`symgraph`** — `rmcp` MAJOR jump: v2.2.0 → v3.2.0 (skip). `matchit` v0.8.4 → v0.8.6 (minor, can apply separately):
```bash
cd ~/repos/symgraph && git checkout -b maintenance/deps-2026-09-05 \
  && cargo update matchit && cargo test \
  && git add Cargo.lock && git commit -m 'chore(deps): update matchit v0.8.4 → v0.8.6' \
  && git push -u origin maintenance/deps-2026-09-05
```

---

## Go Repos (`go list -u -m all`)

### `go-bazel-container` — up to date ✅

### `ai-dev-container` — 20 minor/patch updates (unapplied since ≥2026-08-01)

| Package | Current | Available |
|---------|---------|-----------|
| `containerd/typeurl/v2` | v2.2.0 | v2.3.0 |
| `cpuguy83/go-md2man/v2` | v2.0.6 | v2.0.7 |
| `docker/go-connections` | v0.7.0 | v0.8.1 |
| `felixge/httpsnoop` | v1.0.4 | v1.1.0 |
| `go-logr/logr` | v1.4.3 | v1.4.4 |
| `rogpeppe/go-internal` | v1.14.1 | v1.16.0 |
| `sirupsen/logrus` | v1.9.3 | **v1.10.2** |
| `otelhttp` | v0.67.0 | v0.71.0 |
| `otel` (×5 packages) | v1.42.0 | **v1.46.0** |
| `golang.org/x/mod` | v0.12.0 | v0.40.0 |
| `golang.org/x/time` | v0.11.0 | v0.15.0 |
| `golang.org/x/tools` | v0.11.0 | v0.49.0 |
| `google/genproto` | 20240903 | 20260904 |
| `google/grpc` | v1.67.0 | **v1.83.2** |
| `google/protobuf` | v1.34.2 | v1.36.12 |
| `pgregory.net/rapid` | v1.2.0 | v1.3.0 |

```bash
cd ~/repos/ai-dev-container && git checkout -b maintenance/deps-2026-09-05 \
  && go get -u ./... && go mod tidy \
  && git commit -am 'chore(deps): weekly dependency updates

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TcCwPyJJfUT3nwmpGMovvM' \
  && git push -u origin maintenance/deps-2026-09-05
```

### `attribute` — 22 minor/patch updates (unapplied since ≥2026-08-01)

| Package | Current | Available |
|---------|---------|-----------|
| `anmitsu/go-shlex` | pseudo | v0.0.0-20200514… |
| `cpuguy83/go-md2man/v2` | v2.0.6 | v2.0.7 |
| `creack/pty` | v1.1.9 | v1.1.24 |
| `gliderlabs/ssh` | v0.2.2 | v0.3.8 |
| `google/go-cmp` | v0.6.0 | v0.7.0 |
| `jessevdk/go-flags` | v1.4.0 | v1.6.1 |
| `kr/pretty` | v0.1.0 | v0.3.1 |
| `pelletier/go-buffruneio` | v0.2.0 | v0.3.0 |
| `pkg/errors` | v0.8.1 | v0.9.1 |
| `sirupsen/logrus` | v1.9.3 | v1.10.2 |
| `stretchr/testify` | v1.12.0 | v1.12.1 |
| `go.yaml.in/yaml/v3` | v3.0.4 | v3.0.5 |
| `golang.org/x/crypto` | v0.52.0 | v0.56.0 |
| `golang.org/x/mod` | v0.12.0 | v0.40.0 |
| `golang.org/x/net` | v0.55.0 | v0.58.0 |
| `golang.org/x/sync` | pseudo | v0.22.0 |
| `golang.org/x/sys` | v0.45.0 | v0.47.0 |
| `golang.org/x/term` | v0.43.0 | v0.45.0 |
| `golang.org/x/text` | v0.37.0 | v0.41.0 |
| `golang.org/x/tools` | v0.11.0 | v0.49.0 |
| `gopkg.in/check.v1` | pseudo | v1.0.0-20201130… |

```bash
cd ~/repos/attribute && git checkout -b maintenance/deps-2026-09-05 \
  && go get -u ./... && go mod tidy \
  && git commit -am 'chore(deps): weekly dependency updates

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TcCwPyJJfUT3nwmpGMovvM' \
  && git push -u origin maintenance/deps-2026-09-05
```

### `bsdoc` — 9 minor/patch updates, 1 MAJOR skipped

**Skip:** `github/copilot-sdk/go` v0.3.0 → **v1.0.13** (major, API changed).

Minor/patch: cpuguy83/go-md2man/v2, go-logr/logr, klauspost/compress v1.18.3→**v1.20.0**, rogpeppe/go-internal, stretchr/testify v1.11.1→v1.12.1, otel×3, go.yaml.in/yaml/v3.

```bash
cd ~/repos/bsdoc && git checkout -b maintenance/deps-2026-09-05 \
  && go get \
    github.com/cpuguy83/go-md2man/v2@latest \
    github.com/go-logr/logr@latest \
    github.com/klauspost/compress@latest \
    github.com/rogpeppe/go-internal@latest \
    github.com/stretchr/testify@latest \
    go.opentelemetry.io/otel@latest \
    go.opentelemetry.io/otel/metric@latest \
    go.opentelemetry.io/otel/trace@latest \
    go.yaml.in/yaml/v3@latest \
  && go mod tidy \
  && git commit -am 'chore(deps): weekly dependency updates

Skipped major: github/copilot-sdk/go v0.3.0 → v1.0.13

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TcCwPyJJfUT3nwmpGMovvM' \
  && git push -u origin maintenance/deps-2026-09-05
```

### `xpa` — 6 minor/patch updates, 2 MAJORS skipped

**Skip:** `antlr/antlr4` v0.0.0 → v4.11.1+incompatible (major); `gopkg.in/check.v1` pre-release → v1.0.0 (major API).

Minor/patch: cpuguy83/go-md2man/v2, mattn/go-colorable v0.1.14→v0.1.15, mattn/go-isatty v0.0.20→v0.0.24, spf13/pflag v1.0.9→v1.0.10, go.yaml.in/yaml/v3, golang.org/x/sys v0.42.0→v0.47.0.

```bash
cd ~/repos/xpa && git checkout -b maintenance/deps-2026-09-05 \
  && go get \
    github.com/cpuguy83/go-md2man/v2@latest \
    github.com/mattn/go-colorable@latest \
    github.com/mattn/go-isatty@latest \
    github.com/spf13/pflag@latest \
    go.yaml.in/yaml/v3@latest \
    golang.org/x/sys@latest \
  && go mod tidy \
  && git commit -am 'chore(deps): weekly dependency updates

Skipped majors: antlr4 v0.0.0→v4.11.1+incompatible, gopkg.in/check.v1 pre-release→v1.0.0

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01TcCwPyJJfUT3nwmpGMovvM' \
  && git push -u origin maintenance/deps-2026-09-05
```

### `fingerprint` — 1 patch (low priority)

`gopkg.in/check.v1` pre-release → v1.0.0 (test dep). Not worth a standalone PR.

---

## JS / Node Repos

### `gitatlas` — handled by Renovate ✅

All pending updates are major (typescript 5→7, vite 6→8, @vitejs/plugin-react 4→6). Renovate has open PR branches for each. No manual action needed — review and merge those PRs.

### `codecity` — 2 minor patches in `frontend/`

- `three`: ^0.185.0 → 0.185.1 (patch)
- `vite`: ^8.1.5 → 8.2.2 (patch)

Low priority given small scope. Check if codecity has a test suite before applying.

---

## No Dependency Manifests (Skipped)

`gb-agent-skills` (no requirements.txt/pyproject.toml found — only scripts/markdown), `forge` (no Cargo.toml at root), `go-bazel-container` (up to date).

---

## Private / Failed to Clone

`draws`, `kaizen-ai`, `drive`, `code-provenance`, `rebel-zebra` — clone returned 403/404. Cannot audit.

---

## Action-Required Summary

| Repo | Type | Urgency | Action |
|------|------|---------|--------|
| `cic` | Rust | ⚠️ High | Fix `rustls-tls` → `rustls` feature in Cargo.toml (3rd week) |
| `ai-dev-container` | Go | ⚠️ Medium | Apply `go get -u` (4+ weeks overdue) |
| `attribute` | Go | ⚠️ Medium | Apply `go get -u` (4+ weeks overdue) |
| `bsdoc` | Go | ⚠️ Medium | Apply selective `go get` (skip copilot-sdk major) |
| `xpa` | Go | ⚠️ Medium | Apply selective `go get` (skip antlr/check majors) |
| `mcp-dep` | Rust | 🔍 Review | `rmcp` v0.3.2 → v3.2.0 (major — review API changes) |
| `symgraph` | Rust | 🔍 Review | `rmcp` v2.2.0 → v3.2.0 (major — skip); matchit minor ok |
| `adoc`, `astgen`, `bsv`, `colab`, `fingerprint-rs`, `genie`, `gitatlas-cli`, `puml` | Rust | ✅ Routine | `cargo update` — all minor/patch |
| `gitatlas` | Node | 📋 Review | Renovate PRs open for typescript-7, vite-8, plugin-react-6 |
| `brake`, `increment`, `tropism` | Rust | ❓ Unknown | Rust 1.98 toolchain can't download in session env — check locally |
